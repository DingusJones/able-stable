# Leave unset to use deterministic fixture mode in the frontend.
VITE_API_URL=http://localhost:8787
# Worker live mode is explicit. Never set false until provider behavior is reviewed.
DEMO_MODE=true
ALLOWED_ORIGIN=http://localhost:5173
# Required before authoritative direct RPC adapters can be enabled:
BASE_RPC_URL=
AAVE_BASE_DATA_PROVIDER=
COMPOUND_BASE_COMET=
