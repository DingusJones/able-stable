import { buildCatalog, allDemoOpportunities } from '../src/data/catalog';
import { assets, chains, coverage } from '../src/data/registry';
import type { Mode } from '../src/domain/types';
type QueueBinding={send(body:unknown):Promise<void>}; type QueueMessage<T>={body:T;ack():void;retry():void}; type MessageBatch<T>={messages:QueueMessage<T>[]}; type ExecutionContext={waitUntil(promise:Promise<unknown>):void}; type ScheduledEvent={scheduledTime:number};
type Bindings={DEMO_MODE?:string;ALLOWED_ORIGIN?:string;INGESTION_QUEUE?:QueueBinding;DB?:unknown;EVIDENCE?:unknown};
const json=(body:unknown,status=200,origin='http://localhost:5173')=>new Response(JSON.stringify(body),{status,headers:{'Content-Type':'application/json; charset=utf-8','Cache-Control':'public, max-age=60, stale-while-revalidate=240','Access-Control-Allow-Origin':origin,'X-Content-Type-Options':'nosniff','Referrer-Policy':'strict-origin-when-cross-origin'}});
function route(request:Request,env:Bindings){const url=new URL(request.url),origin=env.ALLOWED_ORIGIN??'http://localhost:5173';if(request.method!=='GET')return json({error:'Method not allowed'},405,origin);if(url.pathname==='/health')return json({ok:true,mode:'fixture',checkedAt:new Date().toISOString(),dependencies:{d1:'not exercised in fixture mode',r2:'not exercised in fixture mode',queue:'not exercised in fixture mode'}},200,origin);if(url.pathname==='/v1/catalog'){const raw=url.searchParams.get('mode'),mode:Mode=raw==='degen'||raw==='cefi'?raw:'core';return json(buildCatalog(mode),200,origin)}if(url.pathname==='/v1/assets')return json({assets},200,origin);if(url.pathname==='/v1/chains')return json({chains},200,origin);if(url.pathname==='/v1/protocols')return json({coverage},200,origin);if(url.pathname==='/v1/status')return json({state:'partial',mode:'fixture',catalogAsOf:'2026-08-30T12:00:00.000Z',adapters:coverage,notice:'No live D1/R2/Queue bindings were exercised.'},200,origin);if(url.pathname==='/v1/methodology')return json({principles:['Native sources first','Canonical identity, never symbol joins','No silent zero or empty-success fallbacks','Exact links only after identity verification','DeFiLlama is advisory only'],demoMode:true},200,origin);const match=url.pathname.match(/^\/v1\/opportunities\/([^/]+)(\/history)?$/);if(match){const item=allDemoOpportunities().find(x=>x.id===decodeURIComponent(match[1]));if(!item)return json({error:'Opportunity unavailable',reason:'No validated canonical opportunity matches this ID.'},404,origin);return json(match[2]?{opportunityId:item.id,window:url.searchParams.get('window')??'7d',observations:[{at:item.evidence.retrievedAt,baseApr:item.baseApr,baseApy:item.baseApy,demo:true}],notice:'Fixture mode contains one deterministic observation.'}:item,200,origin)}return json({error:'Not found'},404,origin)}
export default {fetch:route,scheduled:async(_event:ScheduledEvent,env:Bindings,ctx:ExecutionContext)=>{if(env.INGESTION_QUEUE)ctx.waitUntil(Promise.all(['aave','morpho','compound','moonwell','kamino','solend'].map(adapterId=>env.INGESTION_QUEUE!.send({adapterId,requestedAt:new Date().toISOString()}))))},queue:async(batch:MessageBatch<{adapterId:string}>)=>{for(const message of batch.messages)message.body.adapterId?message.ack():message.retry()}};
